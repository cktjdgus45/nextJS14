import { title } from 'process';
import React from 'react';

export const metadata = {
    title: "profile",
}

const page = (props) => {
    return (
        <>
            about me
        </>
    )
}
export default page;